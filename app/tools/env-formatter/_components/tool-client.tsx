const EnvFormatterClient = () => {
  return (
    <div className="rounded-lg border border-border bg-muted/30 p-6 text-center text-muted-foreground">
      .env Formatter tool — coming soon.
    </div>
  );
};

export default EnvFormatterClient;
